
User wants a ready-to-paste prompt/spec to give to the Connect & Flow project so the chatbot AI consumes the new `os-status-public` contract correctly. This is a documentation/handoff deliverable, not a code change. No plan needed for implementation — just deliver the prompt.

I'll structure it as a single artifact the user can paste into the Connect & Flow project's chat. It should contain:
1. Context (what changed and why)
2. Endpoint details (URL, auth, payload)
3. Full response contract (publico vs interno)
4. Decision tree for the AI to compose messages
5. Tone/copy guidelines (no delay days to client, no "contact store", differentiate ready vs in-progress)
6. Internal escalation rules (when delay matters, how to flag to operator)
7. Concrete message examples per scenario
8. Edge cases (not found, multiple OSs, entregue)

Will present in a single block the user can copy. Since READ-ONLY mode and this is just delivering text, no presentation-plan tags needed for "implementation"... but instructions say plans go in presentation-plan tags. This is a deliverable, not an implementation. I think it's fine to just answer directly with the prompt since it's a handoff artifact, not a build task.

Actually re-reading: "Present plans for implementation tasks". This isn't an implementation task on THIS project — it's producing a spec for another project. I'll just answer directly with the prompt content.
